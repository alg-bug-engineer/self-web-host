# AI 原生一代课程页部署预检｜2026-08-12

- 检查时间：2026-08-12 04:00（Asia/Shanghai）
- 当前决策：**保持阻断（preflight only）**
- 外部写入：无；未提交、未推送、未部署、未启用课程页渠道链接

## 当前证据

- 本地 Next.js 生产构建成功，包含 `/ai-native-generation` 静态路由。
- 02:37 的完整回归为 39 个非 UI 测试全部通过，Playwright 桌面与移动端 18 项通过、2 项按既有条件跳过、0 失败；04:00 后新增改动已分别通过部署审计、内容周历/月度矩阵、运营包、课程视频、NotebookLM 研究队列/登记器和公众号发布门禁定向测试。
- 课程页直接引用的 37 个生产静态资产全部存在，共 183.95 MiB；没有单文件达到 100 MiB。
- 12 份 `.srt` 只作为本地字幕归档，不被页面引用，也不再要求进入生产部署包；对应 12 份 WebVTT 仍属于生产必需资产。
- 16 个运行时/公开配置文件和 6 个发布验收文件全部存在；范围由独立部署清单固定。
- 生产主页与 `/ai-news/` 当前返回 HTTP 200；生产课程页 `https://ai-knowledgepoints.cn/ai-native-generation` 返回 HTTP 404。
- 渠道链接继续保持 `hold_until_course_page_public`，课程内测继续保持 `intake_only` 且付款关闭。

## 当前阻断

1. `deploymentAuthorized` 仍为 `false`，没有收到本轮课程页与静态资产部署的明确授权。
2. 工作区有 94 项改动，现有部署脚本会停止；这些改动包含用户和本活动的未跟踪文件，不能自动批量纳入。
3. 37 个生产静态资产均未被 Git 跟踪。
4. 16 个运行时/公开配置文件中有 6 个未跟踪，6 个发布验收文件中有 2 个未跟踪。
5. 生产课程页仍为 404，尚未达到 HTTP 200 上线标准。

## 获得授权后才执行

1. 按 `ops/campaigns/ai-native-generation-30d-deployment-scope.json` 人工审查本活动应纳入的 16 个运行时/公开配置文件、6 个验收文件和 37 个生产静态资产；继续排除 `docs/著作.jpeg`、12 份本地 `.srt` 归档和未入选即梦候选图。
2. 重新运行完整测试、生产构建与部署审计。
3. 通过现有受控部署流程提交、推送和部署。
4. 核验课程页 200、canonical、12 个 MP4 Range 206、12 个 WebVTT、12 张海报及匿名转化事件。
5. 全部通过后才把渠道链接切换为 `active`；付款门禁仍单独处理。

生产 404 是尚未部署的真实结果，不能通过降低验收标准或发布占位链接消除。
